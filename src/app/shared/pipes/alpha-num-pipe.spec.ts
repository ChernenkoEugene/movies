import { AlphaNumPipe } from './alpha-num-pipe.';

describe('AlphaNumPipePipe', () => {
  it('create an instance', () => {
    const pipe = new AlphaNumPipe();
    expect(pipe).toBeTruthy();
  });
});
