import { TitlesValidatorDirective } from './titles-validator.directive';

describe('TitlesValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TitlesValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
